import React from 'react';
import { storiesOf } from '@storybook/react';

import {{pascalCase name}} from './{{pascalCase name}}';

storiesOf('{{pascalCase name}}', module)
  .addParameters({
    info: {
      text: 'TODO',
    },
  })
  .add('{{pascalCase name}} default', () => <{{pascalCase name}} />);
